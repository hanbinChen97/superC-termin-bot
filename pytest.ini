[pytest]
# ===== Test Markers =====
markers =
    unit: 纯单元测试，无任何外部依赖（无网络、无数据库）
    e2e: Mock 网络的 E2E 测试（使用 respx），完全离线
    integration: 真实网络集成测试（使用 vcrpy 录制/回放），默认离线回放

# ===== 默认跳过 integration 测试（避免意外打真实网络）=====
addopts = -m "not integration"
